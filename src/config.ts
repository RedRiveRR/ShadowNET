/**
 * ShadowNet — Central Configuration
 * Tüm sabit değerler ve versiyon bilgileri burada yönetilir.
 */

export const APP_NAME = 'ShadowNet';
export const APP_VERSION = '9.0';
export const APP_CODENAME = 'Global Intelligence';
export const APP_FULL_TITLE = `${APP_NAME} V${APP_VERSION} — ${APP_CODENAME}`;
export const APP_USER_AGENT = `${APP_NAME}/${APP_VERSION} IntelHub`;
