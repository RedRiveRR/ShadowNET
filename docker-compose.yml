version: '3.8'

services:
  shadownet:
    build: .
    ports:
      - "4173:4173"
    env_file:
      - .env
    restart: unless-stopped
    labels:
      - "com.shadownet.description=Global Intelligence & Tactical Radar"

